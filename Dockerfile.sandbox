FROM alpine:3.20

# No root, no setuid binaries, minimal attack surface.
RUN apk add --no-cache busybox && \
    find / -perm /4000 -exec chmod u-s {} + 2>/dev/null || true && \
    find / -perm /2000 -exec chmod g-s {} + 2>/dev/null || true

USER nobody
WORKDIR /workspace

ENTRYPOINT ["sh"]
